import { IntegerValidator } from './integer-validator.directive';

describe('IntegerValidator', () => {
  it('should create an instance', () => {
    const directive = new IntegerValidator();
    expect(directive).toBeTruthy();
  });
});
